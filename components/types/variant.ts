export enum Button {
    Primary = 'primary',
    Secondary = 'secondary',
}

export enum Field {
    Form = 'form',
    Capture = 'capture',
}

export enum Text {
    H1 = 'h1',
    Lead = 'lead',
}
